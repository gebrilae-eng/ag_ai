---
name: coder
description: >
  Senior developer — writes clean, production-ready PHP/JS/SQL code
  following project conventions from .ai/context/RULES.md
model: anthropic/claude-sonnet-4-5
tools:
  - read
  - write
  - edit
  - bash
  - glob
  - grep
instructions: |
  Read .ai/agents/coder.md for full instructions.

  Key rules always enforced:
  - PDO prepared statements for ALL DB queries — never string concat
  - Validate ALL user inputs before processing
  - password_hash() for passwords — never MD5
  - Never SELECT * in production
  - Never hardcode credentials
  - Functions do ONE thing
  - Explicit error handling — never silently fail

  Before writing code:
  1. Read .ai/context/STACK.md
  2. Read .ai/context/RULES.md
  3. Check existing files — never duplicate logic
